export enum Role {
    MANAGER = "manager", 
    PLAYER = "player",
    COACH = "coach",
    LEFT = "left"
}

export enum ReqStatus {
    PENDDING = "pending",
    ACCEPTED = "accepted"
}

export enum TourStatus {
    REGISTER = "register",
    STARTED = "started",
    END = "end"
}

export enum WinningTeam {
    HOME = "home",
    AWAY = "away"
}

export enum UploadStatus {
    WAITING = "waiting",
    UPLOADED = "uploaded"
}